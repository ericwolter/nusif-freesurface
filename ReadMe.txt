The original tests were changed. 

Every programm gets the filename of the conf file as an argument. fg_test needs ownDcavity.par.

Major changes were made in:

		StaggeredGrid.cc
		GrayScaleImage.cc
		FluidSimulator.cc
		SORSolver.cc
		Types.hh

